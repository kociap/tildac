fn mul(x: i32) -> void {
    var variable0: i32 = a;
    var variable1: T<U<u64>, V<i64>>= variable0;
    if var2 {
        var var_inside_if1: i32 = -1234569432;
        var var_inside_if2: bool = true && variable0 || true;
    }

    {
        var j: i32 = 8477 * 12 / 8;
        if function() + 15 || function_single(var2) || function_single(false && var_false && (var_true || var_or)) {
            function_multi(var0 + -748830238, var0 - -748830238, nested_fn(1, true));
        }

        if variable0 || true && false {

        }
    }

    do {
        var do_while_var: i32 = do_while;
    } while false;

    while var3 {
        b;
    }
}

fn main(argc : i64, argv:c8) -> i32 {
    // Comment
    /**/
    /* Multiline
     * comment */
}
