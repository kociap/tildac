fn mul(x: i32) -> void {
    var variable0: i32 = a;
    var variable1: T<U<u64>, V<i64>>= variable0;
    if var2 {
        var var_inside_if: i32 = var2;
    }

    {
        if true {
            true;
        }
    }

    do {
        var do_while_var: i32 = do_while;
    } while true;

    while var3 {
        b;
    }
}

fn main(argc : i64, argv:c8) -> i32 {
    // Comment
    /**/
    /* Multiline
     * comment */
}
